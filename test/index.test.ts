/* ns__file unit: standard, comp: test/index.test.ts */
/* ns__start_section imports */
import test from 'ava';
/* ns__custom_start customImports */
/* ns__custom_end customImports */

const output = require('../src/index')
/* ns__end_section imports */

/* ns__custom_start general */
test('general', t => {
  // replace with whatever you'd like.  To start over with the default,
  // remove everything in the `general` section incluing the `ns-custom-start`
  // and `ns-custom-end` lines.
    t.is(1,1) // this is just to make sure you have at least one assertion in your test.
});
/* ns__custom_end general */
